/// c2-pty: PTY manager stub — Phase 2 implementation

pub struct PtyManager;

impl PtyManager {
    pub fn new() -> Self { Self }
}

impl Default for PtyManager {
    fn default() -> Self { Self::new() }
}
