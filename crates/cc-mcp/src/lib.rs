/// cc-mcp: MCP client stub — Phase 4 implementation

pub struct McpRegistry;

impl McpRegistry {
    pub fn new() -> Self { Self }
}

impl Default for McpRegistry {
    fn default() -> Self { Self::new() }
}
