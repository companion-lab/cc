/// cc-snapshot: Git-based snapshot stub — Phase 2 implementation

pub struct Snapshot;

impl Snapshot {
    pub fn new() -> Self { Self }
}

impl Default for Snapshot {
    fn default() -> Self { Self::new() }
}
